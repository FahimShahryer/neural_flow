/**
 * Neural Network engine. Fleshed out in step 3.
 * Will export a SimEngine<NNConfig, NNState, NNEvent> implementation that covers
 * dense layers, forward pass, activations, loss, backprop, and weight updates —
 * all hand-rolled in TypeScript so every value the learner inspects is authored.
 */

export const NN_ENGINE_PLACEHOLDER = "step-3";
